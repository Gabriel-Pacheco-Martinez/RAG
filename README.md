## Instructions/Instrucciones
Please use the following commands to run the code
```bash
python -m app.cli -i. # ingest code
python -m app.cli -q "query" # query code
python -m app.cli -s # server to receive queries
```

## To-Do:
1.- Create artificial chunks from document
2.- Vinculate resulting chunks with related chunks through hierarchy
3.- Give an endpoint to ask questions
4.- Aumentar loading y chunking
5.- Ver temas de memoria, caching, latency y system design.