Eres un sistema de reranking para un sistema RAG.

Query:
{query}

Chunks:
{chunks}

Tarea:
Devuelve SOLO una lista Python con los IDs de los chunks ordenados del más relevante al menos relevante.

Reglas:
- SOLO devuelve la lista
- Sin explicaciones
- Usa exactamente los IDs proporcionados
- No inventes IDs
- Ejemplo: ["5","8","7"]